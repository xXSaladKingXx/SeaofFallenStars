using System.Diagnostics.CodeAnalysis;

namespace Zana.WorldAuthoring
{
    /// <summary>
    /// This stub intentionally contains no definitions.  In earlier versions of the
    /// project, this file defined a <c>TimelineParticipantEventIdSynchronizer</c>
    /// class.  That implementation has been superseded by
    /// <see cref="TimelineParticipantEventIdSynchronizerExtended"/> in
    /// <c>Assets/Scripts/WorldAuthoring/Runtime/TimelineParticipantEventIdSynchronizerExtended.cs</c>.
    /// Leaving this file present but empty ensures the compiler does not find
    /// duplicate definitions across multiple files while still preserving the
    /// directory structure expected by existing imports.  If you encounter
    /// duplicates at build time, make sure that only this stub and the
    /// Extended implementation remain in your project.
    /// </summary>
    [SuppressMessage("Design", "CA1034:Nested types should not be visible", Justification = "Stub class intentionally left blank.")]
    internal static class RootTimelineParticipantEventIdSynchronizer
    {
        // no content
    }
}